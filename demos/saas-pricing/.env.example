# API Configuration
# URL of the ab-testing-server (Elo) backend
NEXT_PUBLIC_API_URL=http://localhost:3000

# Tenant Configuration
# Default tenant ID for development/demo
# In production, this should be fetched dynamically or configured per deployment
# NEXT_PUBLIC_DEFAULT_TENANT_ID=

# Experiment Configuration
# Default experiment key for simulation demo
# In production, this should be fetched dynamically
# NEXT_PUBLIC_DEFAULT_EXPERIMENT_KEY=
